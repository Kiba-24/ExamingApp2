Что такое поток в Java?
*Объект, который выполняет код параллельно с другими потоками. 
Команда, используемая для создания объектов.
Интерфейс для работы с графическим интерфейсом.
Способ управления памятью в Java.

Какой класс в Java используется для создания потоков?
*Thread 
Runnable
Executor
Process

Что делает метод start() в классе Thread?
*Запускает поток. 
Останавливает поток.
Создаёт новый поток.
Изменяет приоритет потока.

Какой метод необходимо переопределить в классе, который наследуется от Thread?
*run() 
start()
sleep()
join()
Что такое синхронизация в многопоточном программировании?

Процесс предотвращения доступа нескольких потоков к общему ресурсу одновременно. 
*Увеличение производительности потоков.
Запуск потоков в определённой последовательности.
Удаление потоков из памяти.

Какой ключевое слово используется для создания синхронизированного метода в Java?
*synchronized 
static
volatile
atomic

Что делает метод join()?
*Ожидает завершения другого потока. 
Прерывает выполнение текущего потока.
Запускает новый поток.
Синхронизирует два потока.

Какой интерфейс используется для создания потоков с использованием анонимных классов?
*Runnable 
Thread
Executor
Callable

Какой метод используется для временной приостановки потока?
*sleep() 
wait()
stop()
suspend()

Какой класс используется для создания пула потоков?
*ExecutorService 
ThreadPool
Scheduler
PoolManager

Что происходит, если поток вызывает метод wait()?
*Поток освобождает блокировку и ждет уведомления. 
Поток завершает своё выполнение.
Поток возвращается к началу выполнения.
Поток начинает выполняться параллельно.

Что такое deadlock (взаимная блокировка)?
*Состояние, когда два или более потока ждут друг друга, и никто не может продолжить выполнение. 
Состояние, когда поток завершает выполнение.
Состояние, когда поток не может получить доступ к ресурсу.
Состояние, когда все потоки выполняются одновременно.

Какой метод используется для уведомления другого потока о том, что он может продолжить выполнение?
*notify() 
signal()
wakeup()
resume()

Какой модификатор переменной указывает, что она может быть изменена несколькими потоками?
*volatile 
static
final
synchronized

Какой класс позволяет создавать задачи, которые могут быть выполнены в отдельных потоках?
*Callable 
Runnable
Thread
Task

Что делает метод notifyAll()?
*Уведомляет все ожидающие потоки о том, что ресурс освобождён. 
Завершает выполнение всех потоков.
Запускает все потоки одновременно.
Останавливает текущий поток.

Какой метод используется для завершения потока?
*stop() 
exit()
terminate()
finish()

Что такое race condition (гонка)?
*Ошибка, возникающая из-за одновременного доступа нескольких потоков к общему ресурсу. 
Состояние, когда потоки выполняются в неправильном порядке.
Состояние, когда поток не может быть завершён.
Ошибка, связанная с синхронизацией потоков.

Что делает ключевое слово volatile в Java?
*Гарантирует видимость изменений переменной для всех потоков. 
Снижает производительность многопоточной программы.
Создаёт новый поток.
Блокирует доступ к переменной.

Какой метод в классе Thread используется для установки приоритета потока?
*setPriority() 
changePriority()
modifyPriority()
priority()